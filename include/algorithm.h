#ifndef ALGO_H
#define ALGO_H

#include "main.h"
#include <vector>
#include <set>
#include <map>
#include <queue>

std::vector<std::string> uniformCostSearch(const std::string& source, const std::string& destination);

#endif
